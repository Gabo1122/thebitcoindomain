export declare class UniqPrimitiveCollection<T extends keyof any> {
    size: number;
    private hash;
    constructor(list?: Array<T>);
    add(item: T): this;
    has(key: T): boolean;
    toArray(): Array<T>;
}
