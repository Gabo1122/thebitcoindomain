export declare namespace config {
    namespace console {
        type TConsoleMethods = 'log' | 'info' | 'warn' | 'error';
        const LOG_LEVEL: {
            PRODUCTION: number;
            ERRORS: number;
            VERBOSE: number;
        };
        let logLevel: number;
        const methodsData: Record<TConsoleMethods, {
            save: boolean;
            logLevel: number;
        }>;
    }
}
