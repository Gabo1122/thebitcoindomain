"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const BasePart_1 = require("./BasePart");
const config_1 = require("./config");
const ts_utils_1 = require("ts-utils");
class StringDatePart extends BasePart_1.BasePart {
    constructor(config, path) {
        super(config, path);
        this.dateProcessor = ts_utils_1.date(this.options.outPattern || config_1.OUT_DATE_PATTERN);
    }
    getValue(data) {
        let date;
        if (data instanceof Date) {
            date = data;
        }
        if (typeof data === 'number') {
            date = new Date(data);
        }
        if (date) {
            return this.dateProcessor(date);
        }
        return null;
    }
}
exports.StringDatePart = StringDatePart;
