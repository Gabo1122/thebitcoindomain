export declare class Iterator<T> {
    private _list;
    private _step;
    constructor(some: ArrayLike<T>);
    next(): IArrayStep<T>;
}
export interface IArrayStep<T> {
    done: boolean;
    value?: T;
}
