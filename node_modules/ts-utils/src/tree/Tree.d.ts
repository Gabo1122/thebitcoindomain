import { BaseTree } from './BaseTree';
import { ITreeData, ITreeOptions } from './interface';
export declare class Tree<T> extends BaseTree<T> {
    private _childHash;
    constructor(data: ITreeData<T>, options?: ITreeOptions<T>);
    where(data: Partial<T>): Array<BaseTree<T>>;
    registerChild(child: BaseTree<T>): void;
    getPath(id: any): Array<string | number> | void;
    getRoot(): this;
    find(id: string | number): BaseTree<T>;
}
